#!/bin/sh -euxv

music_dir=$(grep ^music_directory "${HOME}"/.config/mpd/mpd.conf | cut -d"\"" -f2)

mpc_current_info=$(mpc current -f "%file%" | xargs dirname | sed -n 1p)

img_dir="${music_dir}/${mpc_current_info}/Folder.jpg"

notify-send "$(mpc status)" -i "${img_dir}" &
